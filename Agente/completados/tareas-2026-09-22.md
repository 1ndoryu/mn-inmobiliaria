# Completadas 2026-09-22

## Puestos + residencia en inmuebles y solicitudes (229A-1)
- **Migraciones:** `20260922000010_inmuebles_puestos_residencia` y
  `20260922000011_solicitudes_puestos_residencia`: `puestos INTEGER NOT NULL
  DEFAULT 0 CHECK (puestos >= 0)`, `residencia TEXT NOT NULL DEFAULT ''`
  (down incluido). Aplicadas sobre `glory_backend_inmobiliaria`.
- **Código:** `models/inmueble.rs` + `models/solicitud.rs`,
  `repositories/inmueble.rs` + `repositories/solicitud.rs` (`COLUMNAS`),
  `services/inmueble.rs` + `services/solicitud.rs`;
  `handlers/chat_tools.rs` expone `puestos`+`residencia` en
  `buscar_inmuebles` y `detalle_inmueble`; `handlers/ia.rs` literal con
  separadores (`1_726_233_600`).
- **Datos:** relleno verificado `SELECT titulo,puestos,residencia`: Villa
  Icabarú 4, Orquídea 3, Villa Tocoma 2, Caroní Plaza 1 +
  `Residencias Caroní Plaza`; Riberas y galpón sin dato en descripción.
- **Evidencia:** `fmt`, `check --all-targets`, `clippy --all-targets
  -- -D warnings` limpios; `test --lib` 14 passed. Front en repo hermano
  (`INMOBILIARIA` commit `6d74b3b`).
- **Pendiente:** el `:3000` en ejecución corre un binario anterior sin estas
  claves (el JSON público aún no las trae); relanzar para ver
  puestos/residencia en vivo. No se tocó por posible sesión paralela
  (suscriptores) usando el servidor.
