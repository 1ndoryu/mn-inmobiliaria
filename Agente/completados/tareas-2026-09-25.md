# Tareas completadas — 2026-09-25

## 259A-1 — Panel Imágenes: paginación + IDs visibles + restaurar original
- **Qué:** grupos de inmuebles paginados (4 por página, 3 páginas con 11
  inmuebles, paginador arriba y abajo, página derivada sin efecto);
  cada tarjeta muestra el ID de fila del original y de la mejorada
  (mono, con `title` completo); botón Restaurar con confirmación que
  borra la mejorada del servidor (DELETE por id), refresca la lista y
  limpia la copia local (queda pendiente; en automático se re-encola).
  Sin cambios de backend: `MejoraServidor.id` + `Inmueble.idsFotos[]`
  se mapean en `remotoADominio`; `sincronizarFotos` re-lee tras re-subir
  porque la subida solo devuelve URL, no id.
- **Archivos:** `frontend/src/domain/inmueble.ts`, `frontend/src/data/inmuebles/api.ts`
  (`remotoADominio`, `sincronizarFotos`, `eliminarMejorada`),
  `frontend/src/features/imagenes/pagina-imagenes.tsx` (`Paginador`, IDs),
  `frontend/src/features/imagenes/tarjeta-foto-mejora.tsx` (IDs + Restaurar),
  `frontend/src/features/imagenes/vista-imagenes.tsx` (paso de `alRestaurar`),
  `frontend/src/app/app-admin.tsx` (`restaurarMejorada`).
- **Evidencia:** `tsc -b` limpio, `oxlint` 0 errores 18 warnings (baseline),
  `vite build` verde (`index-6aX7pngw.js`, `app-admin-MDfYR6Zv.js`).
  Deploy `20260924_221714-pre_deploy_service` (26605865 bytes, build 547s,
  health 200; único aviso E17 pre-existente). Prod sirve el bundle nuevo
  (`index-l7CJm7Nq.js`). Verificado en `/admin` Imágenes en prod:
  `Página 1 de 3` → clic Siguiente → `Página 2 de 3`, 72 etiquetas
  `ID <uuid>` en página 2 y botón Restaurar presente en cada tarjeta con
  mejorada. El clic de Restaurar NO se probó (borrado real en prod: borra
  la mejorada del servidor; requiere autorización explícita).
- **Gotchas:** el `read` devolvió una versión obsoleta de
  `pagina-imagenes.tsx` (162 líneas con `Images`/`reintentos` requerido vs
  122 reales con `useMemo`/`ImageIcon`/`reintentos?`); detectado porque el
  `edit` falló el match, confirmado con bytes crudos + `git status`
  (lección en `lecciones-aprendidas.md`); el `useEffect` de clamp de página
  sumaba un warning `set-state-in-effect` (19 vs 18): página vigente
  derivada durante el render, sin efecto.
- **Sentinel:** no aplica (sin gate declarado en este repo).
- **GLORY:** No aplica (lógica específica del panel admin inmobiliario).

## 259A-2 — Admin móvil: pestañas a menú inferior fijo estilo app
- **Qué:** la fila de botones bajo la cabecera (Inmuebles/Imágenes/
  Mensajes/Publicidad, desbordaba a 360px) pasa a barra inferior fija
  solo en móvil (`fixed inset-x-0 bottom-0`, `grid-cols-4`, icono +
  etiqueta, activa en primario, `aria-current`, respeta
  `env(safe-area-inset-bottom)`); el contenido reserva `pb-24` en móvil.
  Escritorio intacto (menú lateral). Nuevo `BotonTab` local en layout.
- **Archivos:** `frontend/src/app/layout.tsx`.
- **Evidencia:** `tsc -b` limpio, `oxlint` 0 errores 18 warnings
  (baseline), `vite build` verde (`app-admin-CdvJlK9Y.js`,
  `index-UzyDgIFb.js`). Revisado en local por la usuaria
  (`http://127.0.0.1:5199/admin`, backend `:3000`). Sin deploy.
- **Sentinel:** no aplica.
- **GLORY:** No aplica.

## 259A-3 — Tarjeta de foto: acciones a menú contextual + descargas
- **Qué:** los botones de la tarjeta (Reintentar/Cancelar/Restaurar)
  pasan a menú contextual de 3 puntos (mismo patrón Base UI que
  tabla-inmuebles); +2 acciones: Descargar original y Descargar mejorada
  (solo si hay mejorada), vía `descargarUrl` (blob temporal; el atributo
  `download` se ignora entre orígenes). Nombre derivado del último
  segmento de la URL, saneado para Windows. Fallo de descarga visible en
  la tarjeta, nunca silenciado.
- **Archivos:** `frontend/src/features/imagenes/tarjeta-foto-mejora.tsx`
  (reutiliza `@/platform/descarga` y `@/components/ui/dropdown-menu`,
  sin dependencias nuevas).
- **Evidencia:** `tsc -b` limpio, `oxlint` 0 errores 18 warnings
  (baseline), `vite build` verde (`app-admin-Ccf38p4v.js`,
  `index-CQWYWh9x.js`). Revisado en local por la usuaria. Sin deploy.
- **Sentinel:** no aplica.
- **GLORY:** No aplica (específico del panel Imágenes).

## 259A-4 — Limpieza: borrar repo legado INMOBILIARIA, conservar MN-Inmobiliaria
- **Qué:** `INMOBILIARIA/` (front standalone, rama `master` sincronizada
  con su remoto; prod nunca lo usó) eliminada del disco. Previo: rescate
  `inmobiliaria-rescate-20260915-1205.json` (19.728.604 bytes, solo-local)
  copiado a `area-trabajo/respaldos/` (verificado por tamaño); test
  one-shot `tests/importar_rescate.rs` (159A-2, `#[ignore]`, fase ya
  ejecutada) eliminado porque apuntaba a `../INMOBILIARIA/...` y quedaría
  roto. Cambios locales descartados con la carpeta (`public/llms.txt`
  generado, `.pyc`): nada con valor fuera del backup.
- **Archivos:** `tests/importar_rescate.rs` (eliminado).
- **Evidencia:** `node scripts/run-with-db.mjs check --tests` verde
  (2m23s); `INMOBILIARIA/` ya no existe en disco;   historial remoto
  intacto en `1ndoryu/INMOBILIARIA@master` por si hiciera falta.
- **Sentinel:** no aplica.
- **GLORY:** No aplica.

## Datos prod: precio Calas Suites + Arivana a alquiler (2026-09-25)
- **Qué:** solo-datos vía API prod (sin código ni deploy):
  `51a37a8a…` (Calas Suites) `precio 35000 → 75000`;
  `6e6706a7…` (Arivana) `precio 0 → 1500`, `operacion venta → alquiler`
  (PUT parcial `UpdateInmuebleRequest`).
- **Evidencia:** PUT admin 200 en ambos; `/api/public/inmuebles`
  devuelve `75000.00/venta` y `1500.00/alquiler`.
- **Sentinel:** no aplica. **GLORY:** No aplica.
