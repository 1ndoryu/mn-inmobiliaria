# Completadas 2026-09-24

## Incidencia "Servidor no disponible" + fix sync-env Stripe (239A-1)
- **Que:** home 200 con `Todo (0)` y `Servidor no disponible` pese a API sana; causa = bundle con
  `http://127.0.0.1:3000` horneado (sin `ARG VITE_API_URL` en template ni var en servicio).
  Defecto de herramienta: `sync-env push` exigia trio Stripe aun con `--only` de claves permitidas.
- **Archivos:** `coolify-manager-rs` `config/templates/Dockerfile.rust` (`ARG/ENV VITE_API_URL`,
  `CACHE_BUST=3`), `src/commands/sync_env.rs` + `src/commands/sync_env_helpers.rs`
  (`push_acotado_exime_requeridas` + test); plan deploy, seccion incidencia post-deploy.
- **Gotchas:** `sccache 0.15.0` no entiende `check-cfg` (compilar sin `RUSTC_WRAPPER`);
  shim `cargo` bloquea todo salvo gate (usar `rustup run stable cargo` con
  `CARGO_TARGET_DIR=C:\tmp\glory-target\coolify-manager`); `?per_page=50` devuelve
  `{items,total,page,per_page}` (contar `items`, no raiz); fallbacks `|| localhost` muertos
  en prod pero siguen como literales en el bundle.
- **Evidencia:** `sync-env push --only VITE_API_URL` 1 var OK; `deploy-service` 6/6 OK;
  bundle `index-EZwYRSQX.js` con `VITE_API_URL:https://mn-inmobiliaria.com`;
  `/api/public/inmuebles` `total=11` fotos `218`; `health ok v=0.1.0` `http_ok=true app_ok=true`.
- **Sentinel:** no aplica (repos sin gate declarado; validado con fmt+clippy+197 tests en manager).
- **GLORY:** fix agnostico quedo en `coolify-manager-rs` (`d908b94`, `3008e65`).

## Optimizacion PageSpeed movil + SEO (249A-1) + fix gzip SPA (249A-2)
- **Que:** head SEO (titulo Puerto Ordaz, canonical, OG/Twitter, JSON-LD, favicon),
  `robots.txt`, preload heroe, `presentacion.jpg` 3 MB -> 502 KB, admin tras
  `React.lazy`, thumbs modal lazy, `updated_at`+`?v=`+ETag/304/immutable en fotos,
  `CompressionLayer` gzip, `Cache-Control` SPA, `/sitemap.xml` dinamico, thumbs
  320px q70 (subir/borrar/backfill perezoso). 249A-2: el fallback iba despues de
  los layers y axum no lo comprimia -> movido antes + predicado que excluye imagenes.
- **Archivos:** `frontend/index.html`, `robots.txt`, `presentacion.jpg`, `App.tsx`,
  `app/app-admin.tsx`, `domain/inmueble.ts`, `caja-inmueble.tsx`,
  `modal-detalle-publico.tsx`, `Cargo.toml` (tower-http compression-gzip, image),
  `handlers/mod.rs` (gzip, fallback SPA, sitemap), `handlers/uploads.rs` (ETag,
  backfill thumbs), `repositories/inmueble.rs` (`tocar_inmueble`,
  `ultima_modificacion_publica`), `services/inmueble.rs` (miniatura).
- **Gotchas:** `/*` dentro de comentarios (ej. `` `/assets/*` ``) abre bloque anidado
  -> E0758 (dos veces); Coolify construye `origin/main` (`gloryBranch: main`), no
  `inmobiliaria`: flujo push rama -> merge ff a main -> push main -> deploy;
  `C:\tmp\bin\coolify-manager.exe` (2026-09-18) sirve porque deploy solo sincroniza
  compose y el Dockerfile viaja en el repo; C: con 77 MB libres bloqueaba `cargo test`
  (LNK1180) -> limpieza `limpiador-pc` (~15.5 GB) deja ~20 GB.
- **Evidencia:** gate verde (tsc, build, fmt, check, clippy `-D warnings`, test 15/15);
  commits `41fbf468`+`1740ebe5` en `inmobiliaria` y `main`; deploy 6/6 health 200;
  prod: bundle `index-CLwUJYEY.js` sin admin + chunk lazy, JS gzip 389668->125804,
  HTML 3090->1217, API 11938->3257, sitemap 200 xml, robots 200, foto immutable+ETag
  sin gzip, thumb 12901 bytes JPEG, `total=11` fotos `218`. Pendiente: rerun PageSpeed
  (API con 429) y F8 SEO profundo.
- **Sentinel:** no aplica (sin gate declarado).
- **GLORY:** no aplica (logica especifica del producto).

## Bundle publico: preload muerto fuera + `cn` local (249A-3)
- **Que:** el visualizador mostro `react-dom` > src > base-ui > react > `cn` >
  lucide en el chunk publico. Hallazgo: `PresentacionCaja` (heroe 502 KB) no se
  renderiza y su preload competia con JS/CSS -> eliminado (se conserva
  `og:image`). `cn` (paquete, motor propio ~41 KB raw) sustituido por `cn`
  local (`clsx`+`tailwind-merge`, ya dependencias): JS 626.71 KB gzip 192.73 KB
  (-1.8 KB gzip). Visualizer revertido y desinstalado.
- **Archivos:** `frontend/index.html` (preload fuera), `frontend/src/lib/utils.ts`
  (`cn` local), 7 `frontend/src/components/ui/*.tsx` (`from "@/lib/utils"`),
  `frontend/package.json` + lock (fuera `cn` y `rollup-plugin-visualizer`).
- **Gotchas:** `C:\tmp\bin\coolify-manager.exe` usa otro config por defecto ->
  pasar `-c` con el `settings.json` del proyecto; `Invoke-WebRequest`
  autodescomprime (header `Content-Encoding` vacio no es regresion);
  `frontend/public/llms.txt` lo regenera el prebuild: no commitear.
- **Evidencia:** `tsc` (en build) OK, `oxlint` 0 errores (2 warnings
  preexistentes `only-export-components`); commit `0a255daa` en `inmobiliaria`
  y `main`; `deploy-service --skip-backup` + `health http_ok=true app_ok=true`;
  prod sin `rel=preload`, bundle nuevo `index-BolcIDwy.js`. PSI API sigue en
  429 (cuota anonima del dia agotada): rerun pendiente por el usuario o manana.
- **Sentinel:** no aplica (sin gate declarado).
- **GLORY:** no aplica (logica especifica del producto).

## 249A-4b+249A-4d: thumbs 160px y catalogo agente dinamico (2026-09-24)
- **Que:** `miniatura()` 320->160 JPEG q70 con clave nueva `min160-<uuid>.jpg`;
  legado `thumb-` aceptado al servir (backfill + borrado, `delete_foto` borra
  ambas); `miniaturaDe` del front apunta a `min160-`. `GET /llms.txt` y
  `GET /.well-known/ai-catalog.json` dinamicos (11 publicados, URNs saneadas
  `[^A-Za-z0-9._]`->`_`); test `urn_inmueble_sanea_slug_a_alfabeto_ard`.
  Eliminados `frontend/scripts/generar-llms.mjs`, `prebuild`/`llms`,
  `frontend/public/llms.txt`, y deps `lighthouse`, `@radix-ui/*`, `shadcn`
  (`package-lock` -4856 lineas). Nota: el gotcha del prebuild en 249A-3 queda
  obsoleto (ya no hay prebuild: el backend sirve ambos endpoints).
- **Archivos:** `src/handlers/mod.rs` (+211: `llms_txt`, `ai_catalog`,
  `urn_inmueble`, `linea`, test), `src/handlers/uploads.rs` (backfill+legado),
  `src/services/inmueble.rs` (`min160-`+legada), `frontend/src/domain/inmueble.ts`,
  `frontend/package.json`+lock.
- **Gotchas:** `cargo` pela bloqueado (usar `scripts/run-*.mjs` o
  `rustup run stable cargo` con `CARGO_TARGET_DIR` fuera del arbol);
  `SELECT version` de sqlx exige `PGPASSWORD`; checksums `_sqlx_migrations`
  locales se realinean desde el archivo (CRLF); ARD exige `updatedAt`
  formato `date-time` (el tester oficial lo valida); `Start-Job` no expone
  el puerto (usar `Start-Process -PassThru` + pidfile).
- **Evidencia:** `cargo check`+`clippy -D warnings`+`fmt --check` OK,
  `test --lib` 16/16, `tsc`+`oxlint` (0 errores, 18 warnings preexist.)+`vite
  build` OK; tester oficial ARD 0 errores; local `:3123` thumb 160x120 JPEG +
  legado borrado; commit `c8ae5341` (`inmobiliaria`->ff `main`->`origin/main`);
  `deploy-service` build 517s health 200; prod `/llms.txt` 200 6092 B,
  ai-catalog 200 11 entries, `min160-` 200 jpeg, `total=11`. PSI API sigue 429.
- **Sentinel:** no aplica (sin gate declarado).
- **GLORY:** no aplica (logica especifica del producto).

## 249A-4a+249A-4c: fallback metric-matched, LCP eager, discovery agentico y lazy overlays (2026-09-24)
- **Que:** `Söhne-relevo` (400 `local Arial` 99.45 %/104 %/23.4 %, 700
  `local Arial Bold` 103.53 %/103.7 %/28.9 %) en `--font-soehne` (CLS
  footer 0.195); primera tarjeta `prioritaria` (`eager`+`fetchpriority`
  high, resto `lazy`) contra LCP 1.8s; `Agentmap:` en `robots.txt` +
  `<link rel=ai-catalog>` en `index.html` (3/4 -> 4/4 navegacion
  agentica); `ModalDetallePublico,ModalFiltros,ModalLogin,ModalPublicar,
  ChatVisitante` tras `React.lazy`+`Suspense` (unused-JS 60.3 KB del
  bundle 121 KB).
- **Archivos:** `frontend/src/index.css`, `frontend/src/features/publica/
  lista/caja-inmueble.tsx`, `frontend/src/features/publica/
  pagina-publica.tsx`, `frontend/public/robots.txt`, `frontend/index.html`.
- **Gotchas:** Geist admin sin relevo (fuera de la ruta LCP publica);
  `dist/index.html`/`robots.txt` se verifican con `grep`, no
  `Select-String -Include` (da vacios falsos).
- **Evidencia:** `tsc` OK, `oxlint` 0 errores (18 warnings preexist.),
  `vite build` 3.42s (chunks `modal-filtros 5.16kB, detalle 6.45kB,
  publicar 7.67kB, chat 11.18kB, dialog 106.97kB gzip 35.70kB, index
  472.84kB gzip 144.10kB`); commit `80b6ba2e` (`inmobiliaria`->ff
  `main`->`origin/main`); `deploy-service` backup
  `20260924_164930-pre_deploy_service` 26606809 bytes, build 546s,
  health 200; prod `robots.txt` con `Agentmap:`, `/` con
  `rel=ai-catalog`, bundle `index-D8vNccQi.js` + modulepreload
  `inmueble/plantilla-publicidad`. PSI: rerun pendiente por el usuario
  (API 429 ese dia; ultimo manual 88/100/100/100 + 3/4).
- **Sentinel:** no aplica (sin gate declarado).
- **GLORY:** no aplica (logica especifica del producto).
