# Completadas 2026-09-24

## Incidencia "Servidor no disponible" + fix sync-env Stripe (239A-1)
- **Que:** home 200 con `Todo (0)` y `Servidor no disponible` pese a API sana; causa = bundle con
  `http://127.0.0.1:3000` horneado (sin `ARG VITE_API_URL` en template ni var en servicio).
  Defecto de herramienta: `sync-env push` exigia trio Stripe aun con `--only` de claves permitidas.
- **Archivos:** `coolify-manager-rs` `config/templates/Dockerfile.rust` (`ARG/ENV VITE_API_URL`,
  `CACHE_BUST=3`), `src/commands/sync_env.rs` + `src/commands/sync_env_helpers.rs`
  (`push_acotado_exime_requeridas` + test); plan deploy, seccion incidencia post-deploy.
- **Gotchas:** `sccache 0.15.0` no entiende `check-cfg` (compilar sin `RUSTC_WRAPPER`);
  shim `cargo` bloquea todo salvo gate (usar `rustup run stable cargo` con
  `CARGO_TARGET_DIR=C:\tmp\glory-target\coolify-manager`); `?per_page=50` devuelve
  `{items,total,page,per_page}` (contar `items`, no raiz); fallbacks `|| localhost` muertos
  en prod pero siguen como literales en el bundle.
- **Evidencia:** `sync-env push --only VITE_API_URL` 1 var OK; `deploy-service` 6/6 OK;
  bundle `index-EZwYRSQX.js` con `VITE_API_URL:https://mn-inmobiliaria.com`;
  `/api/public/inmuebles` `total=11` fotos `218`; `health ok v=0.1.0` `http_ok=true app_ok=true`.
- **Sentinel:** no aplica (repos sin gate declarado; validado con fmt+clippy+197 tests en manager).
- **GLORY:** fix agnostico quedo en `coolify-manager-rs` (`d908b94`, `3008e65`).

## Optimizacion PageSpeed movil + SEO (249A-1) + fix gzip SPA (249A-2)
- **Que:** head SEO (titulo Puerto Ordaz, canonical, OG/Twitter, JSON-LD, favicon),
  `robots.txt`, preload heroe, `presentacion.jpg` 3 MB -> 502 KB, admin tras
  `React.lazy`, thumbs modal lazy, `updated_at`+`?v=`+ETag/304/immutable en fotos,
  `CompressionLayer` gzip, `Cache-Control` SPA, `/sitemap.xml` dinamico, thumbs
  320px q70 (subir/borrar/backfill perezoso). 249A-2: el fallback iba despues de
  los layers y axum no lo comprimia -> movido antes + predicado que excluye imagenes.
- **Archivos:** `frontend/index.html`, `robots.txt`, `presentacion.jpg`, `App.tsx`,
  `app/app-admin.tsx`, `domain/inmueble.ts`, `caja-inmueble.tsx`,
  `modal-detalle-publico.tsx`, `Cargo.toml` (tower-http compression-gzip, image),
  `handlers/mod.rs` (gzip, fallback SPA, sitemap), `handlers/uploads.rs` (ETag,
  backfill thumbs), `repositories/inmueble.rs` (`tocar_inmueble`,
  `ultima_modificacion_publica`), `services/inmueble.rs` (miniatura).
- **Gotchas:** `/*` dentro de comentarios (ej. `` `/assets/*` ``) abre bloque anidado
  -> E0758 (dos veces); Coolify construye `origin/main` (`gloryBranch: main`), no
  `inmobiliaria`: flujo push rama -> merge ff a main -> push main -> deploy;
  `C:\tmp\bin\coolify-manager.exe` (2026-09-18) sirve porque deploy solo sincroniza
  compose y el Dockerfile viaja en el repo; C: con 77 MB libres bloqueaba `cargo test`
  (LNK1180) -> limpieza `limpiador-pc` (~15.5 GB) deja ~20 GB.
- **Evidencia:** gate verde (tsc, build, fmt, check, clippy `-D warnings`, test 15/15);
  commits `41fbf468`+`1740ebe5` en `inmobiliaria` y `main`; deploy 6/6 health 200;
  prod: bundle `index-CLwUJYEY.js` sin admin + chunk lazy, JS gzip 389668->125804,
  HTML 3090->1217, API 11938->3257, sitemap 200 xml, robots 200, foto immutable+ETag
  sin gzip, thumb 12901 bytes JPEG, `total=11` fotos `218`. Pendiente: rerun PageSpeed
  (API con 429) y F8 SEO profundo.
- **Sentinel:** no aplica (sin gate declarado).
- **GLORY:** no aplica (logica especifica del producto).
