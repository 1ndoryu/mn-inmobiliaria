# Tareas 2026-09-16

## 169A-1 — F5 chat visitante con IA (backend)

- **Que:** `glory-agent` como dependencia por path (`../glory-agent`);
  migración `20260916000005_agent_chat` (tablas `agent_*`, DDL idéntico a
  `glory-agent/migrations/0001_chat.sql`); módulo `handlers/chat.rs`
  (`PromptConfig` inmobiliario con `AGENTE_CONTACTO`, `AgentState` con pool,
  `transport::routes().with_state`); anidado en `/api` vía `nest_service`
  (`nest` exige mismo estado; verificado en fuente axum 0.7.9) →
  `/api/agent/{ws,messages,history}`. Sin tools en v1 (F6 en `glory-agent`).
- **Archivos:** `Cargo.toml`, `Cargo.lock`, `src/handlers/chat.rs`,
  `src/handlers/mod.rs`, `migrations/20260916000005_agent_chat.up/down.sql`,
  `roadmap.md`, `Agente/planes/plan-chat-inmobiliaria-2026-09-16.md`
  (movido a `planes/completados/`).
- **Gotchas:** (1) `nest` con estados distintos no compila (E0308):
  `nest_service("/api", router_con_estado)` sí despoja prefijo igual que
  `nest`. (2) Hallazgo raíz en `glory-agent`: `session_id` de cliente sin
  fila → FK en primer mensaje; fix en el crate (`ensure_session`
  idempotente en `process_incoming`, commit `ca53feb`), no parche local.
  (3) `C:\tmp` a 14.2 GB (techo 7): purgado `glory-target/debug`+`tmp` →
  5.8 GB. (4) Servidor dev 05:57 bloqueaba link+puerto: detenido tras
  verificar que era binario pre-F5; smoke con binario nuevo y luego
  cleanup (puerto 3000 libre; rearrancar dev habitual).
  (5) `OPENCODE_GO_API_KEY` ausente en este backend → smoke en degradado
  (persiste+realtime, `reply:null`); el usuario debe fijarla + real
  `AGENTE_CONTACTO` en `MN-Inmobiliaria/.env`.
- **Evidencia:** `fmt --check` + `clippy --all-targets -- -D warnings` +
  `cargo test` verdes; boot real contra BD de rama (migración 00005
  aplicada): `GET history`→`[]`, `POST messages`→`ok:true seq:1`,
  `GET history`→1 mensaje `[client]`, `GET ws` sin upgrade→400 (no 404).
- **Sentinel:** no aplica (gate del proyecto es clippy+tests; sin
  supresiones nuevas).
- **GLORY:** lo reutilizable ya vivía en `glory-agent`; aquí solo
  identidad del producto + wiring. Nada que mover.

## 169A-5+169A-6 - Admin Mensajes+Config y tarjeta contacto (front) + E2E vivo (cierre chat)

- **Que:** front en repo `INMOBILIARIA` (commits `fae846e` 990 ins y
  `fa745e0` 174 ins): `cliente-admin` (sesiones/historial/responder/patch/
  config) + hooks bandeja (poll 5 s) y config (diff PUT) + vistas
  Mensajes/Config + nav; frente visitante 169A-1 + tarjeta
  contacto/escalado (`pedirInfo`/`enviarContacto`, auto-apertura si pide
  humano). E2E en vivo contra binario dev de esta rama (ver plan).
- **Archivos (este repo):** `roadmap.md` (169A-5/6 [x]),
  `Agente/planes/plan-chat-atencion-2026-09-16.md` (Hecho + a
  `planes/completados/`), leccion E2E detached. Sin cambios de codigo
  backend: el E2E valido el contrato 169A-4 sin tocarlo.
- **Gotchas:** (1) `[ESCALATE]` literal del visitante NO escala: el
  escalado lo dispara la IA via tool `escalar_a_humano` o staff al tomar
  el hilo; sin clave IA el mensaje solo persiste (degradado honesto).
  (2) `Start-Job` no sobrevive entre llamadas de terminal: build largo en
  sincrono con timeout amplio; servidor detached con `Start-Process` +
  probe `/api/health`, nunca como espera ciega. (3) Tras `ChildProcess.
  kill` ambiguo, comprobar `Get-Process` antes de relanzar (el servidor
  si habia arrancado).
- **Evidencia:** E2E vivo 2026-09-16 (health 200, seq 1/2, history 2
  client, info defaults, contacto `{ok:true}`, staff 401); front
  `oxlint` 0 errores + `tsc -b + vite build` verde (commits citados).
- **Sentinel:** no aplica (sin supresiones nuevas).
- **GLORY:** nada que mover (identidad del producto + wiring).
