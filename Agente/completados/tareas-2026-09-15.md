## 159A-1 — API catálogo inmuebles + roles owner/admin
- **Que:** migraciones `users.role` + tablas `inmuebles`/`fotos`; CRUD admin (`/api/admin/inmuebles`, fotos, `PATCH publicacion`); pública solo lectura (`/api/public/inmuebles`, detalle por slug, filtros tipo/operacion/precio); register como bootstrap del primer owner + alta de admins solo por owner (`POST /api/admin/users`); `AppError::Forbidden` (403).
- **Archivos:** `migrations/20260915000001_roles.up/down.sql`, `migrations/20260915000002_inmuebles.up/down.sql`, `src/models/inmueble.rs`, `src/models/mod.rs`, `src/models/user.rs`, `src/errors/mod.rs`, `src/repositories/inmueble.rs`, `src/repositories/user.rs`, `src/repositories/mod.rs`, `src/services/inmueble.rs`, `src/services/auth.rs`, `src/services/mod.rs`, `src/handlers/inmuebles.rs`, `src/handlers/public.rs`, `src/handlers/users.rs`, `src/handlers/auth.rs`, `src/handlers/mod.rs`
- **Gotchas:** sqlx sin feature decimal → `DOUBLE PRECISION`/f64; `QueryBuilder` solo concatena fragmentos fijos; slug con reintento ante 23505; `PUT` de axum es método (no importar `put` suelto); `cargo run` en Windows tarda en dar readiness (esperar 30-60s, mejor binario directo).
- **Sentinel:** no aplica (sin gate en este repo)
- **GLORY:** específico inmobiliaria, no agnóstico
- **Evidencia:** `cargo fmt --check` limpio, `cargo check`/`clippy --all-targets -- -D warnings` PASS, `cargo test` 1/1 (slugify); funcional contra binario: register 201→403, draft vacío 201 (slug `inmueble`), tipo inválido 422, publicar 200→ pública 0→1, slug no publicado 404, alta admin 201, alta por admin 403, cleanup 204. Quedan en BD `f2owner/f2admin@example.com` (owner+admin de pruebas para F3).

## 159A-2 (F3) — Admin contra API + import real del rescate
- **Que:** cliente REST del admin (api.ts, sesion en sessionStorage, login/bootstrap owner), repositorio de inmuebles contra la API, subida/sync de fotos por bytes, publicar/retirar desde el visor; script `importar-rescate.mjs` corregido (sin `r.user.role`, ext por magic-bytes) e import real ejecutado: 5 inmuebles, 82 fotos, publica en 0.
- **Archivos:** `scripts/importar-rescate.mjs`, `Agente/planes/completados/plan-f3-2026-09-15.md` (plan cerrado y movido). El codigo del admin vive en INMOBILIARIA (commit separado alli).
- **Gotchas:** auth responde sin campo `user`; dataURLs mejoradas con mime declarado distinto a los bytes; filas viejas con fotos huerfanas en disco (wipe fotos+inmuebles, se conserva users, reimport limpio).
- **Sentinel:** no aplica (sin gate en este repo)
- **GLORY:** especifico inmobiliaria, no agnostico
- **Evidencia:** import EXIT 0 (Admin 5/82, Publica 0, resumen 5/48/34/14); live: login 200, admin total=5 todos publicado=false, foto 200 image/jpeg 113KB; `cargo build` 35s PASS.
