# Tareas completadas — 2026-09-26

## 269A-1 — Documentar mp-float en MN-Inmobiliaria
- **Qué:** nuevo `Agente/documentacion/integraciones/asistente-marketplace-mp-float-2026-09-26.md`
  con arquitectura (catalog/assistant/float/service), reglas (precio-o-nada,
  1 opción, plantillas, histéresis, auto-copia con foco, debug), historial
  OP-33 (`44a93d1`, `78106ba`, `5b2a581`, `1bcec99`), relación con este repo
  (el precio lo pone la API pública) y antecesor [259A-5]. Hechos verificados
  por grep en `opencode-propio` (`FLOAT_VERSION = 3`, `focusedKey`,
  `autoCopiarSiFoco`, `CONTACTO`).
- **Archivos:** `Agente/documentacion/integraciones/asistente-marketplace-mp-float-2026-09-26.md`, `roadmap.md`.
- **Sentinel:** no aplica. **GLORY:** No aplica (el código vive en `opencode-propio`).

## 269A-2 — Precio Riberas 50000 → 43000 (local hecho, prod pendiente)
- **Qué:** `12bf11d6-82e4-4b8d-8968-7a29d2e75c8c` (Casa en venta en Riberas
  del Caroní, venta) a `43000` en BD local `glory_backend_inmobiliaria`
  (SQL directo 1 fila, verificado por SELECT). Prod sigue en `50000.00`
  (verificado en `/api/public/inmuebles`): falta la clave admin actual
  (`MN_ADMIN_CLAVE` no existe en el equipo); al recibirla, guardarla como
  env de usuario y aplicar PUT parcial + verify pública.
- **Archivos:** `roadmap.md` (ningún código; cambio solo-datos).
- **Sentinel:** no aplica. **GLORY:** No aplica.

## 269A-2 (prod) — Riberas 43000 en producción + descripción
- **Qué:** PUT parcial `UpdateInmuebleRequest` a
  `12bf11d6-…`: `precio 50000 → 43000` y descripción
  `50.000 → 43.000 dólares` (el texto visible aún citaba el precio viejo).
  Pública devuelve `precio 43000.0`, `updated_at 2026-09-26`.
- **Evidencia:** login 200 (token 165 chars), PUT 200 ambos campos, GET
  público verificado.
- **Sentinel:** no aplica. **GLORY:** No aplica.

## 269A-3 — Rotación clave admin prod
- **Qué:** el usuario no tenía la clave actual: nueva de 28 chars generada
  con `secrets.token_urlsafe`, hash argon2id vía Python (`argon2-cffi`;
  el backend acepta cualquier param PHC), aplicada con
  `run-sql --name inmobiliaria` (`UPDATE 1`, solo `admin@admin.com`).
  Binario del manager recompilado antes (el target lo había purgado
  `GloryTmpSweep`; `cargo build --release`, 13m14s). Clave guardada como
  variable de entorno de usuario `MN_ADMIN_CLAVE` para reutilizarla siempre
  (no va al repo; el `.sql` de `C:\tmp` solo contiene el hash).
- **Evidencia:** login prod 200 con la nueva clave (token 165 chars).
- **Archivos:** `roadmap.md` (ningún código).
- **Sentinel:** no aplica. **GLORY:** No aplica.
